<%= render 'shared/nav' %>
<div class="page-container">
  <div class="content">
    <div class="container">
      <ul class="list-inline page-content">
        <li>
          <a href="/" title="home"><i class="fa fa-home fa-2x"></i></a>
        </li>
        <li class="lead">
          >
        </li>
        <li>
          <i class="fa fa-bar-chart fa-2x" title="Promoções"></i>
        </li>
      </ul>
      <div class="panel panel-default col-xs-12">
        <div class="panel-body">
          <h1><i class="fa fa-bar-chart"></i> Principais ofertas</h1>
          <h4>Veja quais são as melhores promoções do nosso estoque.</h4>
        </div>
      </div>
      <% if @vehicles.blank? %>
        <div class="panel panel-default col-xs-12">
          <div class="panel-body">
            <h5>Infelizmente não temos nenhuma promoção disponível para hoje.</h5>
          </div>
        </div>
      <% else %>
        <%= render partial: 'vehicles/vehicles', locals: {vehicles: @vehicles} %>
      <% end %>
    </div>
  </div>
  <%= render 'shared/footer' %>
</div>
